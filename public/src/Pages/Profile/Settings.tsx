import { User } from "../../Constants";
import "../../CSS/Profile.css";

export default function Settings({ user }: { user: User | null }) {
    return (<div className="settings-parent">
        <p>Coming soon!</p>
    </div>);
}